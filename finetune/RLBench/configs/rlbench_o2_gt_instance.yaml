# O2 experiment: train low-rank feature adapters and multiscale fusion heads.
# Use with --init_checkpoint and --train_oracle_adapter_only.
# Pass the augmented Oracle replay root with --train_replay_storage_dir.

exp_id: rlbench_o2_gt_instance
tasks: all
bs: 48
num_workers: 4
epochs: 50
train_iter: 38400
global_batch_size: 192

gradient_checkpointing: False
efficient_paligemma_forward: True
gpu_paligemma_preprocessing: False
flash_attention_2: False
freeze_gemma_prefix_layers: 18
freeze_multimodal_projector: True

sample_distribution_mode: task_uniform

train_visualization:
  enabled: True
  interval: 500
  save_png: True
  tensorboard: True
  output_dir: train_visualizations

use_oracle_objects: True
oracle_max_objects: 32
oracle_num_points: 512
oracle_prior_hidden_channels: 64
oracle_prior_adapter_rank: 16
oracle_prior_multiscale_fusion: True

peract:
  lr: 4e-5
  transform_augmentation_xyz: [0.125, 0.125, 0.125]
  transform_augmentation_rpy: [0.0, 0.0, 45.0]

rvt:
  place_with_mean: False
  img_aug: 0.0
  oracle_prior_mode: o2_gt_instance
  oracle_prior_sigma: 2.0
  # Compatibility only; ignored while relation mode is enabled.
  oracle_prior_active_role: auto
  oracle_prior_strict: False
  oracle_prior_relation: True
  oracle_log_base_loss: True
